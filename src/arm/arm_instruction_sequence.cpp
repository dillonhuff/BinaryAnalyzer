#include "arm_instruction_sequence.h"

std::string arm_instruction_sequence::to_string() {
  std::string instruction_string = "";
  for (auto& instr : instructions) {
    
  }
  return instruction_string;
}
